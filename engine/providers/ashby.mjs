// Ashby provider stub — intentionally empty.
// Only implement when you have verified a legitimate public feed (or a permitted integration).
// Do NOT turn this project into an indiscriminate scraper.
//
// TODO: add verified public feed, e.g. https://api.ashbyhq.com/posting-api/job-board/<token>

/**
 * @param {object} entry
 * @param {object} ctx
 * @returns {Promise<Array>}
 */
export async function fetch(_entry, _ctx) {
  return [];
}
