// Workable provider stub — intentionally empty.
// Only implement when you have verified a legitimate public feed (or a permitted integration).
//
// TODO: add verified public feed, e.g. https://apply.workable.com/api/v1/accounts/<token>/jobs

/**
 * @param {object} entry
 * @param {object} ctx
 * @returns {Promise<Array>}
 */
export async function fetch(_entry, _ctx) {
  return [];
}
